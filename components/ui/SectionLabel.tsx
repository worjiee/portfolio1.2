export function SectionLabel({ children }: { children: React.ReactNode }) {
  return <p className="mb-3 font-[family-name:var(--font-mono)] text-[0.72rem] font-medium tracking-[0.15em] text-[var(--accent)] uppercase">{children}</p>;
}
